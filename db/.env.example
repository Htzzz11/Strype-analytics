# Copy this file to `.env` in this same `db/` folder.
# `.env` is gitignored; safe to put real passwords there.

# Container ports
MYSQL_HOST_PORT=3306

# Database + credentials (created on first container startup)
MYSQL_DATABASE=strype_analytics
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_USER=strype
MYSQL_PASSWORD=strypepass
