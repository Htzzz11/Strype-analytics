# Copy to `.env` in this folder (gitignored).

# If `npm start` fails with EADDRINUSE, pick another free port and set the same URL in the project root .env as VITE_ANALYTICS_INGEST_URL.
INGEST_PORT=8787

MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=strype
MYSQL_PASSWORD=strypepass
MYSQL_DATABASE=strype_analytics
